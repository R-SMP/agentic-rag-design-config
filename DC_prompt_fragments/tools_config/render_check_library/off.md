*(Mesh quality checks are OFF this session: the render step returns the three
views and the mesh's bounding box, but no watertightness / volume /
degenerate-face numbers — so there are none to report.)*
