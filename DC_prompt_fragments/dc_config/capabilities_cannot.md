- Mesh editing or post-processing of any kind (booleans, welding, remeshing,
  hole filling, normal repair, component pruning, fillets, struts, supports),
  export in any format other than OBJ (no STL, STEP, IGES …), extra camera
  angles, arbitrary section cuts through the mesh, or higher-resolution
  renders — the parameter set, tessellation and the three fixed 3D views are
  not negotiable.
- Performance or structural analysis: RPM, thrust, flow, pressure,
  efficiency, CFD, FEA, stress, load, tolerance, material selection.
- Moving or converting files on the user's behalf — no e-mailing,
  re-hosting, or conversion to another format.
