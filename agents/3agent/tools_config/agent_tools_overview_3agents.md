Beyond the roster above: the Requirements Analyst's ``view_images`` is not
limited to the renders just produced — it also loads a PRIOR attempt's
renders.  When progress across cycles is the question, name those
earlier render paths in the hand-off alongside the new ones.
