When the plan is a blade-sections task — render the sections, not the full 3D
propeller — say so explicitly in your hand-off to the Design Engineer: tell it to
render the blade sections.  Write `parameters.json` and create the attempt as
usual; the Design Engineer will render the sections from that file instead of
generating the mesh.

