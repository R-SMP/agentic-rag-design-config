When the plan is a blade-sections task — render the sections, not the full 3D
propeller — say so explicitly in your hand-off to the Tool Caller: tell it to
render the blade sections.  Open the attempt and write `parameters.json` as
usual; the Tool Caller will render the sections from that file instead of
generating the mesh.
