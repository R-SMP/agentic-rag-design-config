### Domain hard rules
- The $parameter_count named parameters are the ONLY design levers:
  geometry changes only by changing them.<<MESH_ON>>
- The ONLY mesh metrics are watertightness, volume and degenerate-face
  count.<</MESH_ON>>
