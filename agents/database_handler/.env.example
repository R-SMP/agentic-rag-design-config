# Optional per-agent LLM override for the Database Handler.
# Leave this file unfilled to use the shared default in agents/.env.
# Copy to ".env" and edit when you want this agent on a different model.
#
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# MODEL_NAME=gpt-5-mini
