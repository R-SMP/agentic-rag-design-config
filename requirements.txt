langchain>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.1.0
langgraph>=0.3.0
compute-rhino3d>=0.12.0
rhino3dm>=8.0.0
DracoPy>=0.4.0
python-dotenv>=1.0.0
numpy>=2.0.0
trimesh>=4.0.0
pyrender>=0.1.45
# NOTE: pyrender 0.1.45 hard-pins ``PyOpenGL==3.1.0`` (a release from
# May 2014 whose OSMesa bindings predate ``OSMesaCreateContextAttribs``
# and break offscreen rendering in the Stage A container).
# We force-upgrade PyOpenGL via a separate ``pip install --no-deps``
# step in the Dockerfile AFTER this file's resolver run completes —
# putting it here would make pip's resolver fail the build outright
# (it correctly refuses to satisfy both ``==3.1.0`` and ``>=3.1.7``
# inside a single resolution pass).  See Dockerfile for the override.
# pyvista is the ALTERNATIVE render backend selected by
# workflow_settings.RENDER_LIBRARY = "pyvista".  ``tools/__init__.py``
# imports the pyvista wrapper UNCONDITIONALLY at module load time
# (line 17, ``from tools.render_mesh.render_mesh_pyvista import ...``),
# so the container needs pyvista on PYTHONPATH even when the trimesh
# backend is the active one.  Without it the Streamlit app fails to
# import.  Lazy-importing the pyvista backend would let us drop this
# dep when only trimesh is configured — defer that refactor unless
# image size becomes a real concern (pyvista pulls in VTK, ~100 MB).
Pillow>=10.0.0
pyvista>=0.43.0
# Stage A web UI (Phase 3).  Streamlit is the sole entry point in
# Stage A — no FastAPI front-door (see cloud_architecture_notes.md C2).
streamlit>=1.39.0
