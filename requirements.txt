langchain>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.1.0
langgraph>=0.3.0
compute-rhino3d>=0.12.0
rhino3dm>=8.0.0
DracoPy>=0.4.0
python-dotenv>=1.0.0
numpy>=2.0.0
trimesh>=4.0.0
pyrender>=0.1.45
# pyrender pulls in PyOpenGL as a transitive dep with the constraint
# ``PyOpenGL~=3.1.0`` — which on a fresh resolver run gives PyOpenGL
# 3.1.0 (May 2014).  3.1.0's OSMesa bindings are incomplete and miss
# ``OSMesaCreateContextAttribs``, which the offscreen renderer needs.
# This breaks rendering in the Stage A Docker container (which sets
# ``PYOPENGL_PLATFORM=osmesa`` for headless rendering) even though
# native Windows runs are unaffected — Windows resolves OpenGL via
# WGL and OSMesa is never invoked.  Pin a newer PyOpenGL so the
# OSMesa bindings are complete.  ``pyrender~=3.1.0`` still permits
# anything in 3.1.x.
PyOpenGL>=3.1.7
# pyvista is the ALTERNATIVE render backend selected by
# workflow_settings.RENDER_LIBRARY = "pyvista".  ``tools/__init__.py``
# imports the pyvista wrapper UNCONDITIONALLY at module load time
# (line 17, ``from tools.render_mesh.render_mesh_pyvista import ...``),
# so the container needs pyvista on PYTHONPATH even when the trimesh
# backend is the active one.  Without it the Streamlit app fails to
# import.  Lazy-importing the pyvista backend would let us drop this
# dep when only trimesh is configured — defer that refactor unless
# image size becomes a real concern (pyvista pulls in VTK, ~100 MB).
Pillow>=10.0.0
pyvista>=0.43.0
# Stage A web UI (Phase 3).  Streamlit is the sole entry point in
# Stage A — no FastAPI front-door (see cloud_architecture_notes.md C2).
streamlit>=1.39.0
