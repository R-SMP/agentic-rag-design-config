langchain>=0.3.0
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.1.0
langgraph>=0.3.0
compute-rhino3d>=0.12.0
rhino3dm>=8.0.0
DracoPy>=0.4.0
python-dotenv>=1.0.0
numpy>=2.0.0
trimesh>=4.0.0
pyrender>=0.1.45
# NOTE: pyrender 0.1.45 hard-pins ``PyOpenGL==3.1.0`` (a release from
# May 2014 whose OSMesa bindings predate ``OSMesaCreateContextAttribs``
# and break offscreen rendering in the Stage A container).
# We force-upgrade PyOpenGL via a separate ``pip install --no-deps``
# step in the Dockerfile AFTER this file's resolver run completes —
# putting it here would make pip's resolver fail the build outright
# (it correctly refuses to satisfy both ``==3.1.0`` and ``>=3.1.7``
# inside a single resolution pass).  See Dockerfile for the override.
# pyvista is the ALTERNATIVE render backend selected by
# workflow_settings.RENDER_LIBRARY = "pyvista".  ``tools/__init__.py``
# imports the pyvista wrapper UNCONDITIONALLY at module load time
# (line 17, ``from tools.render_mesh.render_mesh_pyvista import ...``),
# so the container needs pyvista on PYTHONPATH even when the trimesh
# backend is the active one.  Without it the web app fails to
# import.  Lazy-importing the pyvista backend would let us drop this
# dep when only trimesh is configured — defer that refactor unless
# image size becomes a real concern (pyvista pulls in VTK, ~100 MB).
Pillow>=10.0.0
pyvista>=0.43.0
# Cloudflare R2 mirror for the post-session database.  R2 is
# S3-compatible, so the standard boto3 client works once the
# endpoint URL is pointed at <account_id>.r2.cloudflarestorage.com.
# See agents/shared/r2_uploader.py for the wiring and
# README "Cloudflare R2" for the env vars required.
#
# Scope note (architecture v4, 2026-06-01): R2 is now the failure
# escape hatch for the DH (safety folder when DATABASE_ENTRY_MAX_RETRIES
# is exhausted on a Postgres INSERT) plus the store for mesh / render /
# user-provided-image artefacts that don't fit in Postgres. Q+A text
# is NOT mirrored to R2 in the happy path. See
# extra_utilities/db_design/database_and_RAG_architecture.md §3.5.
boto3>=1.34.0

# PostgreSQL client (psycopg 3) + pgvector adapter — for the Railway
# Postgres backend (chunks RAG corpus, sessions, attempts, parameter
# inventory, rag_queries log). The [binary,pool] extras bundle the
# precompiled C extension and the built-in AsyncConnectionPool.
# See extra_utilities/db_design/database_and_RAG_architecture.md for
# the full schema and retrieval design.
psycopg[binary,pool]>=3.2
pgvector>=0.3

# HTTP client for the Google Cloud Vision OCR engine
# (agents/shared/ocr/google_vision.py — a plain REST call to
# images:annotate, authenticated with GOOGLE_CLOUD_VISION_API_KEY).
# Transitively present today via other deps; declared explicitly
# because the OCR engine imports it directly.
requests>=2.31.0
