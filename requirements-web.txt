# F4 JavaScript web interface (local / experimental) — the backend
# bridge for web_app.py.  NOT used by the Stage A Streamlit container
# (cloud_architecture_notes.md C2 keeps Streamlit as the sole Railway
# entry point).  Install alongside the main requirements:
#
#   pip install -r requirements.txt -r requirements-web.txt
#
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Voyage AI multimodal embedding SDK.  Used by (1) the
# extra_utilities/embedding_tests/ harness (the "Embedding tests"
# side-menu view) and (2) the multimodal chunks_mm DB layer
# (agents/shared/voyage_mm.py — voyage-multimodal-3.5 @ 2048 dims,
# dual-written on save + backfilled).  Requires VOYAGE_API_KEY in the
# environment.  Floor bumped to 0.4.0: voyage-multimodal-3.5 +
# output_dimension need a recent SDK (verified working on 0.4.0).
voyageai>=0.4.0
