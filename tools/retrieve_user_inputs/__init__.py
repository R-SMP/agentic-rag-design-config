"""DC-specific tool: retrieve_user_inputs.

Returns user-supplied text + image notes (and optionally image bytes)
for one or more past saved sessions identified by their session_id
slugs.  See ``retrieve_user_inputs.py`` for the public API and
``DC_prompt_fragments/tools_config/retrieve_user_inputs.md`` for the
agent-facing description.
"""
